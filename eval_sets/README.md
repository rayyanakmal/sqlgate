# Eval sets — provenance

Generated by `scripts/build_eval_sets.py` (seed 20260819) — deterministic and regenerable.

| Layer | Records | Source |
|---|---|---|
| corpus | 19 | **Authored golden data** (2026-08-19), tagged. No public corpus matches the toy schema — Spider/BIRD reference their own databases — so the corpus is documented authored work, executed-verified, not fabricated. |
| golden | 19 | Same records + `expected_result_hash` = SHA-256 of the EXECUTED gold result (execution-verified ground truth). |
| adversarial | 17 | Authored must-reject negatives with expected reasons. |
| mutation | 11 | Seeded paraphrases of golden questions. |

Schema: 8 tables. Rebuild: `uv run python scripts/build_eval_sets.py`
